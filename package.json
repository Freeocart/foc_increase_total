{
  "name": "foc_increase_total",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "gulp",
    "pack": "bash pack.sh",
    "build": "gulp build",
    "build-all": "npm run build-frontend && gulp build",
    "build-frontend": "cd frontend && GENERATE_SOURCEMAP=false npm run build --prod"
  },
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-zip": "^5.0.1"
  },
  "devDependencies": {
    "yarn": "^1.22.4"
  }
}
